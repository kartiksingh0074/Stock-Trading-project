name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    env:
      # Placeholder values so schema loading / build-time env checks succeed.
      # No real database connection is made during lint/build.
      DATABASE_URL: "mysql://user:pass@localhost:3306/ci"
      BETTER_AUTH_SECRET: "ci-placeholder-secret-min-32-characters-long"
      BETTER_AUTH_URL: "http://localhost:3000"
      FINNHUB_API_KEY: "ci-placeholder"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
